# 11. 配置快照与恢复

![配置历史](../images/snapshots.png)

OKIT 每次切换 Provider / 模型前，都会自动保存当时的配置快照。

1. 进入 **设置 → 配置历史**
2. 按 Agent 筛选（或查看全部），列表显示每个版本的时间
3. 点**查看**打开双栏对比，确认差异后一键**恢复**到该版本
4. 恢复操作本身也会先生成安全快照，可以再恢复回来

误切换、配置写坏、想回到上周的组合——都从这里找回来。
